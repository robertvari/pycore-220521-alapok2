from utilities.math_utils import add_numbers, divide_numbers, multiply_numbers
# from testing import * bad practice


print(add_numbers(2, 2))
print(divide_numbers(2, 2))
print(multiply_numbers(2, 2))